coffee:
  targets:
    - node

yaml:
  targets:
    node:
      - preset: js
        glob:
          - "{src,test}/**/*.yaml"

schema:
  glob: "src/**/schema.yaml"

sky:
  schema:
    type: rune
    glob: "test/**/authorization.yaml"
    auto: false